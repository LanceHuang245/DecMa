/// Centralizes identifiers and versions shared across the application.
class AppConstants {
  const AppConstants._();

  static const appName = 'DecMa';
  // Keep this release name aligned with the version name in pubspec.yaml.
  static const appVersion = '1.0.0';
  static const mcpProtocolVersion = '2024-11-05';
  static const codexClientVersion = '0.147.0';
}
